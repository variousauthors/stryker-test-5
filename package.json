{
  "name": "stryker-test-5",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "stryker": "stryker run"
  },
  "devDependencies": {
    "jest-cli": "^18.0.0",
    "stryker": "^0.18.0",
    "stryker-api": "^0.12.0",
    "stryker-javascript-mutator": "^0.3.2",
    "stryker-jest-runner": "^0.3.0",
    "stryker-webpack": "^0.1.0"
  }
}
